name: test-hello

on:
  workflow_dispatch:
  schedule:
    - cron: "15 12 * * 1"

concurrency:
  group: ${{ github.ref }}
  cancel-in-progress: true

jobs:
  hello-job-1:
    name: hello-job-1
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Say hello
        run: echo "hello ajinkya hi"

  hello-job-2:
    name: hello-job-2
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Say hello
        run: echo "hello ajinkya hi"

  hello-job-3:
    name: hello-job-3
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Say hello
        run: echo "hello ajinkya hi"
